import React from "react";
import Login from '../containers/Login';
import Wrapper from '../containers/Wrapper'

export default function login() {
  return (
    <Wrapper Contenido={<Login/>} />
  )}
